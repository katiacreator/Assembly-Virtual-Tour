<%- include('../partials/html-head') %>
<%- include('../partials/home-nav') %>

<main class="bg-light">
  <div class="container py-5 px-3">
    <div 
      class="row row-cols-2 row-cols-md-3 row-cols-lg-4 row-cols-xl-5 g5 g-sm-3 g-xxl-5 px-sm-5 px-4"
    >
      
        <div class="col">
          <a class="text-decoration-none" href="/profiles/<%= profile._id %>">
            <div class="card shadow-sm">
              <% if (profile.avatar) { %>
                <img
                  class="card-img-top"
                  src="<%= profile.avatar %>"
                  alt="<%= profile.name %>'s avatar"
                  width="100%"
                  height="170"
                />
              <% } else {%>
                <img
                  class="card-img-top"
                  src="/images/icons/user.png"
                  alt="A placeholder avatar"
                  width="100%"
                  height="170"
                />
              <% } %>
              <div class="card-body">
                <p class="card-title fs-5 text-dark mb-0" id="card-name">
                  <%= profile.name %>
                </p>
                <p class="card-title fs-5 text-dark mb-0" id="card-role">
                  <%= profile.role %>
                </p>
                <!-- function generate random number gets called here from js-->
                <p class="card-title fs-5 text-dark mb-0" id="card-badge">
                  <%= profile.badgeId %>
                </p>
                <img src="" alt="barcode">tva logo here
              </div>
            </div>
          </a>
        </div>
    </div>
  </div>
</main>

<%- include('../partials/footer') %>
