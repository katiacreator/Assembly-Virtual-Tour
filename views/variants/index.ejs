<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link 
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" 
    rel="stylesheet" 
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" 
    crossorigin="anonymous"
    >
    <link rel="stylesheet" href="/stylesheets/grid.css" />
    <link rel="stylesheet" href="/stylesheets/welcome.css">
    <title><%= title %></title>
<!-- <title>variantes Card Grid</title> -->
<body>
    <!-- navbar -->
    <section id="variant-database-text" class="typewriter">These are all the known variants that the TVA is actively tracking. They may be affialiated with a person in our database but notice how the records are different. This is due to the chaos of multiple timeline branches!</section>
    <main class="grid">
    <section id="variant-database-grid">
            <!-- for each variant print a profile card -->
        <% variants.forEach( variant => { %>
            <article>
                <!-- Image goes here -->
                <% if (variant.image) { %><%# if an image exists %> 
               <img src="<%= variant.image %>" alt="<%= variant.name %>'s comic book image" height="300px" width="300px" />
               <% } else {%>
                 <img src="◉" alt="placeholder image">
                 <% } %>
               <div class="text">
                 <h2 class="variant-name"><%=variant.name  %></h2>
                 <p class="variant-variantId">File Number: <%=variant.variantId  %></p>
                  <!-- this button will send them to comic details view show comics/:id view -->
        <a class="text-decoration-none btn" id="statsBtn" href="/aliases/<%= alias._id %>">
        <button class="btn btn-danger">View Record</button>
        </a>
        </div>
        </article>
        </a>
        <% }) %>
        </main>
</body>
</html>              